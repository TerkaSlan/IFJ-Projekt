//
// Created by Charvin on 23. 10. 2016.
//

#include "../error.h"

int main() {

	for(int i = 1; i < 10; i++) {
		printError(i, "This is error message #%d\n", i);
	}

	printError(ERR_INTERN, "This is error message #%d\n", 99);
	return 0;
}